"""Bot-substrate detectors for Sysadmin Watchdog — populated in later layers.

L2 ships platform detectors only. Bot-substrate detectors (memory-health
guardian, handoff freshness, tier-config drift) land incrementally as the
absorption plan in the architecture doc and L2 spec play out.
"""
