# 0.2 — Update stale architecture docs · **Tier: Sonnet**

## Context
Two docs describe subsystems that were removed/replaced and now actively mislead a
reader. The README was already corrected (2026-06-09); these two must follow.

## Source of truth (reconcile AGAINST these — do not trust prose, grep the code)
- **`CLAUDE.md`** → the "Arbiter (L1-L6) on-disk layout" and "Signal store" sections
  are accurate. Treat as canonical.
- **`packages/analyzer/arbiter/store.py`** → the real proposal status enum and subdir
  routing (`pending / snoozed / applied / archived`).
- **`packages/analyzer/generators/`** → 26 generator dirs (the "coaches"). Count it.
- **`packages/analyzer/arbiter/routing.py`** → autonomy vs. human-approval routing.

## Task
1. **`docs/architecture.md`** — it still documents a pipeline that no longer exists:
   `analyze.py` / `report.py` / `review.py` with `proposals/{reviewed,deployed}/`
   subdirs and a "pending → reviewed → approved → deployed" flow, plus a stale
   role/script table. Replace the pipeline description, the subdir list, and the
   role table with the **actual** arbiter model (generators write Proposals; monitors
   write Signals linked via `motivating_signals[]`; statuses `pending/snoozed/applied/
   archived`; autonomy gate + human-approval routing per `routing.py`). Keep the
   genuinely-still-true parts (plugin/analyzer/admin package split, account model).
2. **`docs/calibration-schema.md`** — references the removed `maintenance_ratio`
   detector family and `analyze.py` reading outcome_stats. Verify what calibration
   actually does today (grep `packages/analyzer` for the calibration module + current
   detectors) and correct or clearly mark removed pieces.

## Acceptance criteria / proof artifact
- `grep -rniE "analyze\.py|report\.py|proposals/reviewed|proposals/deployed|maintenance_ratio" docs/architecture.md docs/calibration-schema.md`
  returns **only** intentional historical notes (e.g. a dated "removed in …" line), not
  live descriptions.
- Every subsystem named in the two docs exists in code (spot-check 5 by grep).
- Coach/generator count matches `ls packages/analyzer/generators | wc -l`.

## Guardrails
- **Cross-check every claim against code before writing it.** Confabulating a plausible
  pipeline is the failure mode here.
- Surgical edits — preserve accurate sections; don't rewrite wholesale.
- Keep terminology consistent with the corrected README (e.g. "around two dozen coaches",
  not "twelve").
- If you can't determine whether a subsystem still exists, STOP and ask the quarterback.

## Verify
`grep` checks above + read the two docs end-to-end for internal consistency.
